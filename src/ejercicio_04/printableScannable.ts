/**
 * @interface PrintableScannable es una interfaz
 * que declara las dos principales funciones de maquinas
 * imprimir con `print()` y leer con `scan()`.
 */
export interface PrintableScannable {
  print(): void;
  scan(): void;
}

